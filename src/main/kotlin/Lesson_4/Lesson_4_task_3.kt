fun main() {

    val weatherTodaySolar: Boolean = true
    val tentOpen: Boolean = true
    val airHumidity: Int = 20
    val currentTimeOfYear: String = "Winte"

    println(
        "Благоприятные ли условия сейчас для роста бобовых? ${
            (weatherTodaySolar == weatherTodaySolar) && (tentOpen == tentOpen) && (airHumidity == airHumidity) && !(currentTimeOfYear == currentTimeOfYear)
        }"
    )


}

