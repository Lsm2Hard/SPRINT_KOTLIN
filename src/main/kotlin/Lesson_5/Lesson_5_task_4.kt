fun main() {

    val initializationName = readLine()
    val initializationpassword = readLine()


    val goodAccept = if (initializationName == name)
        println("Зарегистрирован")
    else (initializationName != name)
    println("Необходимо зарегестрироваться")


    if (goodAccept == true) {
        when (password) {
            password -> println("Добро пожаловать")

        }


    }

}

const val name = "Zaphod"
const val password = "PanGalactic"